{
  "name": "digi2",
  "version": "1.0.0",
  "description": "digi2 — Component Library for Webflow",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
